# ====================================
# PaperReading Frontend Configuration
# ====================================
# 复制此文件为 .env 并按需修改,所有变量均为可选
# 修改后需重新执行 `npm run build` 才会生效

# ---------- 后端 API 地址 ----------
# 默认使用相对路径 '/api/v1',适合同源部署(前端和后端在同一域名/端口下)
# 若前后端分离部署,改为后端完整地址,如:https://api.example.com/api/v1
# VITE_API_BASE_URL=/api/v1

# ---------- Pexels 壁纸 API(可选) ----------
# 用于个性化欢迎页的网络壁纸:
#   - 配置后:走 Pexels API(无水印、高质量、按关键词搜索)
#   - 不配置:走 LoremFlickr(免费、无需 key、真实摄影图,图片带 license/author 水印)
# 两者均失败时回退到本地 SVG 矢量壁纸
# 获取密钥:https://www.pexels.com/api/
# VITE_PEXELS_API_KEY=
